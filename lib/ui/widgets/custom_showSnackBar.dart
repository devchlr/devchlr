import 'package:flutter/material.dart';

class CustomShowSnackBar extends StatelessWidget {
  const CustomShowSnackBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
